<Window x:Class="CosUI.Views.SettingsOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Width="460" Height="460"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <!-- Styled ComboBox for settings -->
        <Style x:Key="SettingsComboStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderActiveBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6"
                                    IsHitTestVisible="False">
                                <Grid Margin="{TemplateBinding Padding}">
                                    <ContentPresenter x:Name="ContentSite"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      HorizontalAlignment="Left"
                                                      VerticalAlignment="Center"/>
                                    <Path HorizontalAlignment="Right" VerticalAlignment="Center"
                                          Data="M 0 0 L 4 4 L 8 0" Stroke="{DynamicResource TextMutedBrush}"
                                          StrokeThickness="1.5" Margin="4,0,0,0"/>
                                </Grid>
                            </Border>
                            <ToggleButton Focusable="False" ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent"/>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup x:Name="Popup" Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" Focusable="False"
                                   PopupAnimation="Fade">
                                <Border Background="{DynamicResource SurfaceBrush}"
                                        BorderBrush="{DynamicResource BorderActiveBrush}"
                                        BorderThickness="1" CornerRadius="6"
                                        MinWidth="{TemplateBinding ActualWidth}"
                                        MaxHeight="260">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="16" ShadowDepth="0"
                                                          Opacity="0.5" Color="{DynamicResource Shadow}"/>
                                    </Border.Effect>
                                    <ScrollViewer>
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource PrimaryBlueBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Padding" Value="10,7"/>
                        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource BorderActiveBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource BorderActiveBrush}"/>
                                            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Styled CheckBox -->
        <Style x:Key="SettingsCheckStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Border x:Name="Bd" Width="38" Height="20" CornerRadius="10"
                                Background="{DynamicResource BorderSubtleBrush}"
                                BorderBrush="{DynamicResource BorderActiveBrush}"
                                BorderThickness="1" Cursor="Hand">
                            <Ellipse x:Name="Knob" Width="14" Height="14" Margin="2,0"
                                     Fill="{DynamicResource TextMutedBrush}"
                                     HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource PrimaryBlueBrush}"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource PrimaryBlueBrush}"/>
                                <Setter TargetName="Knob" Property="Fill" Value="White"/>
                                <Setter TargetName="Knob" Property="HorizontalAlignment" Value="Right"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource BackgroundBrush}"
            BorderBrush="{DynamicResource BorderSubtleBrush}"
            BorderThickness="1" CornerRadius="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Border Grid.Row="0" Background="{DynamicResource SidebarBgBrush}"
                    CornerRadius="10,10,0,0">
                <Grid Margin="20,0">
                    <TextBlock Text="Settings" FontSize="13" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               VerticalAlignment="Center"/>
                    <Border HorizontalAlignment="Right" VerticalAlignment="Center"
                            Width="20" Height="20" CornerRadius="4" Cursor="Hand"
                            Background="Transparent" MouseLeftButtonDown="Close_Click">
                        <TextBlock Text="✕" FontSize="10"
                                   Foreground="{DynamicResource TextMutedBrush}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <!-- Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="24,20,24,4">

                    <!-- Theme section -->
                    <TextBlock Text="THEME" FontSize="9" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextMutedBrush}" Margin="0,0,0,12"/>
                    <ComboBox Style="{StaticResource SettingsComboStyle}"
                              ItemsSource="{Binding ThemeOptions}"
                              SelectedItem="{Binding SelectedTheme}"
                              Margin="0,0,0,24">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Ellipse Width="10" Height="10" Margin="0,0,10,0"
                                             Fill="{Binding Converter={StaticResource ThemeAccent}}"/>
                                    <TextBlock Text="{Binding Name}" FontSize="12"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <!-- Performance section -->
                    <TextBlock Text="PERFORMANCE" FontSize="9" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextMutedBrush}" Margin="0,0,0,12"/>
                    <Grid Margin="0,0,0,12">
                        <TextBlock Text="Max FPS" FontSize="12"
                                   Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                        <Border HorizontalAlignment="Right"
                                Background="{DynamicResource SurfaceBrush}"
                                BorderBrush="{DynamicResource BorderActiveBrush}"
                                BorderThickness="1" CornerRadius="6" Width="72" Height="32">
                            <TextBox Text="{Binding MaxFps, UpdateSourceTrigger=PropertyChanged}"
                                     Background="Transparent"
                                     Foreground="{DynamicResource TextPrimaryBrush}"
                                     BorderThickness="0" Padding="8,0"
                                     VerticalContentAlignment="Center"
                                     CaretBrush="{DynamicResource AccentBlueBrush}"/>
                        </Border>
                    </Grid>
                    <Grid Margin="0,0,0,24">
                        <TextBlock Text="Unlock FPS" FontSize="12"
                                   Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                        <CheckBox HorizontalAlignment="Right" VerticalAlignment="Center"
                                  IsChecked="{Binding UnlockFps}"
                                  Style="{StaticResource SettingsCheckStyle}"/>
                    </Grid>

                    <!-- Script section -->
                    <TextBlock Text="SCRIPTS" FontSize="9" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextMutedBrush}" Margin="0,0,0,12"/>
                    <Grid Margin="0,0,0,24">
                        <TextBlock Text="Auto Execute" FontSize="12"
                                   Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                        <CheckBox HorizontalAlignment="Right" VerticalAlignment="Center"
                                  IsChecked="{Binding AutoExec}"
                                  Style="{StaticResource SettingsCheckStyle}"/>
                    </Grid>

                    <!-- Console section -->
                    <TextBlock Text="CONSOLE" FontSize="9" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextMutedBrush}" Margin="0,0,0,12"/>
                    <Grid Margin="0,0,0,12">
                        <TextBlock Text="Redirect output" FontSize="12"
                                   Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                        <CheckBox HorizontalAlignment="Right" VerticalAlignment="Center"
                                  IsChecked="{Binding ConsoleRedirect}"
                                  Style="{StaticResource SettingsCheckStyle}"/>
                    </Grid>
                    <Grid Margin="0,0,0,4">
                        <TextBlock Text="Filter" FontSize="12"
                                   Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                        <ComboBox Style="{StaticResource SettingsComboStyle}"
                                  HorizontalAlignment="Right" Width="200"
                                  ItemsSource="{Binding FilterOptions}"
                                  SelectedItem="{Binding ConsoleFilter}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" FontSize="12"
                                               Foreground="{DynamicResource TextSecondaryBrush}"/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </Grid>

                </StackPanel>
            </ScrollViewer>

            <!-- Footer -->
            <Border Grid.Row="2"
                    Background="{DynamicResource SidebarBgBrush}"
                    BorderBrush="{DynamicResource BorderSubtleBrush}"
                    BorderThickness="0,1,0,0"
                    CornerRadius="0,0,10,10"
                    Padding="20,12">
                <Button Content="Done" HorizontalAlignment="Right"
                        Style="{StaticResource PrimaryBtnStyle}"
                        Click="Close_Click"/>
            </Border>
        </Grid>
    </Border>
</Window>
