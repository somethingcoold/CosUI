<UserControl x:Class="CosUI.Controls.StatusDot"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="10" Height="10">
    <UserControl.Resources>
        <Storyboard x:Key="PulseAnim" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="Ring"
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                             From="1" To="2.2" Duration="0:0:1.2"/>
            <DoubleAnimation Storyboard.TargetName="Ring"
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                             From="1" To="2.2" Duration="0:0:1.2"/>
            <DoubleAnimation Storyboard.TargetName="Ring"
                             Storyboard.TargetProperty="Opacity"
                             From="0.6" To="0" Duration="0:0:1.2"/>
        </Storyboard>
    </UserControl.Resources>

    <Grid>
        <Ellipse x:Name="Ring" Width="10" Height="10"
                 RenderTransformOrigin="0.5,0.5"
                 Opacity="0.6">
            <Ellipse.RenderTransform>
                <ScaleTransform/>
            </Ellipse.RenderTransform>
            <Ellipse.Style>
                <Style TargetType="Ellipse">
                    <Setter Property="Fill" Value="{StaticResource StatusOffBrush}"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=IsActive}" Value="True">
                            <Setter Property="Fill" Value="{StaticResource StatusOnRingBrush}"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Ellipse.Style>
        </Ellipse>

        <Ellipse x:Name="Core" Width="7" Height="7"
                 HorizontalAlignment="Center" VerticalAlignment="Center">
            <Ellipse.Style>
                <Style TargetType="Ellipse">
                    <Setter Property="Fill" Value="{StaticResource StatusOffBrush}"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=IsActive}" Value="True">
                            <Setter Property="Fill" Value="{StaticResource StatusOnBrush}"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Ellipse.Style>
        </Ellipse>
    </Grid>
</UserControl>
